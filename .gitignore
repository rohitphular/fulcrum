# macOS
.DS_Store

# IDE
.idea/
.vscode/

# Dev task files
**/dev-tasks/

# Local server PID file (make app-start / app-stop)
.server.pid

# clasp credentials — never commit OAuth tokens
.clasprc.json

# Local configs, secrets, and temp files — never commit
local/

