// =============================================================================
// STARTER MODULE — Config template
// Copy this file to config.js and fill in your Apps Script URL.
// config.js is gitignored — never commit it.
//
// Your PIN is NOT stored here. You enter it in the app on first load.
// It is held in sessionStorage for the current browser tab only.
// =============================================================================

window.CONFIG = {
  SCRIPT_URL: 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec'
};
